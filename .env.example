OPENAI_API_KEY=
FIRECRAWL_API_KEY=
SMTP_PASSWORD=

# Set to development for local change-test site and mutation endpoints.
# Accepted values: development, dev, test (anything else runs in production mode).
APP_ENV=development

# IANA timezone for APScheduler cron jobs (default: Europe/Berlin).
APP_TIMEZONE=Europe/Berlin
